import numpy as np
import random

sizing = random.randrange(10) 

A = np.random.randint(1000, size=sizing)
a=[False, True]
B = np.random.choice(a, sizing)

print(B)
print(A)
Fin = []

for i in range(0,len(A)):
  if(B[i]== True):
    Fin.append(A[i])
  else:
    continue

print(Fin)
